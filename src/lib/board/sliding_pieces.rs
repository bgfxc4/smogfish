use super::helper::Sides;
use super::{Position, Board, Move};

pub fn get_all_moves_bishop(board: &Board, pos: &Position, moves: &mut Vec<Move>) {
}

pub fn get_all_moves_rook(board: &Board, pos: &Position, moves: &mut Vec<Move>) {
}

pub fn get_all_moves_queen(board: &Board, pos: &Position, moves: &mut Vec<Move>) {
}
